# AWS Modules

AWS is the current delivery target for the assessment. These modules are organized by capability so the platform repository can compose environments without spreading raw AWS resources across every environment definition.
